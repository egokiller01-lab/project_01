---
import type { CTA } from "../types";

type Props = {
  eyebrow: string;
  title: string;
  summary: string;
  ctas: CTA[];
  strengths: string[];
  image: {
    src: string;
    alt: string;
    caption: string;
  };
};

const { eyebrow, title, summary, ctas, strengths, image } = Astro.props;
---

<section class="hero">
  <div class="container hero-grid">
    <div class="hero-copy">
      <p class="eyebrow">{eyebrow}</p>
      <h1>{title}</h1>
      <p class="summary">{summary}</p>
      <div class="hero-actions">
        {
          ctas.map((cta) => (
            <a
              class={`button button--${cta.variant ?? "secondary"}`}
              href={cta.href}
            >
              {cta.label}
            </a>
          ))
        }
      </div>
    </div>
    <div class="hero-panel" aria-label="PLACHEM VINA process support strengths">
      <figure class="hero-media">
        <img
          src={image.src}
          alt={image.alt}
          width="1024"
          height="1024"
          loading="eager"
          decoding="async"
        />
        <figcaption>{image.caption}</figcaption>
      </figure>
      <div class="strength-list">
        {strengths.map((strength) => <span>{strength}</span>)}
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    background:
      linear-gradient(90deg, rgba(11, 31, 58, 0.96), rgba(39, 50, 62, 0.88)),
      var(--color-navy);
    border-bottom: var(--border);
    color: white;
    padding-block: var(--space-7);
  }

  .hero-grid {
    display: grid;
    gap: var(--space-6);
  }

  .eyebrow {
    color: var(--color-steel);
    font-size: 0.86rem;
    font-weight: 900;
    letter-spacing: 0.08em;
    margin: 0 0 var(--space-3);
    text-transform: uppercase;
  }

  h1 {
    color: white;
    font-size: clamp(2.35rem, 5vw, 4.35rem);
    letter-spacing: 0;
    line-height: 1.05;
    margin: 0;
    max-width: 13ch;
    overflow-wrap: keep-all;
    word-break: keep-all;
  }

  .summary {
    font-size: clamp(1.05rem, 1.6vw, 1.22rem);
    margin-block: var(--space-5);
    max-width: 56ch;
    color: rgba(248, 250, 252, 0.86);
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-3);
  }

  .hero-panel {
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(228, 233, 239, 0.32);
    border-radius: var(--radius-md);
    display: grid;
    gap: var(--space-4);
    overflow: hidden;
    padding: var(--space-3);
  }

  .hero-media {
    margin: 0;
    position: relative;
  }

  .hero-media img {
    aspect-ratio: 16 / 11;
    border-radius: calc(var(--radius-md) - 2px);
    display: block;
    height: auto;
    object-fit: cover;
    object-position: 50% 50%;
    width: 100%;
  }

  .hero-media figcaption {
    background: rgba(11, 31, 58, 0.88);
    border-radius: var(--radius-sm);
    bottom: var(--space-3);
    color: white;
    font-size: 0.78rem;
    font-weight: 700;
    left: var(--space-3);
    max-width: calc(100% - var(--space-6));
    padding: 0.45rem 0.65rem;
    position: absolute;
  }

  .strength-list {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
    padding: 0 var(--space-2) var(--space-2);
  }

  .strength-list span {
    background: rgba(248, 250, 252, 0.08);
    border: 1px solid rgba(228, 233, 239, 0.28);
    border-radius: var(--radius-sm);
    color: white;
    font-size: 0.86rem;
    font-weight: 800;
    padding: 0.55rem 0.7rem;
  }

  @media (min-width: 64rem) {
    .hero-grid {
      align-items: center;
      grid-template-columns: minmax(0, 1.1fr) minmax(24rem, 0.9fr);
    }
  }

  @media (max-width: 47.99rem) {
    .hero {
      padding-block: var(--space-6);
    }

    h1 {
      max-width: 11ch;
    }

    .hero-media figcaption {
      position: static;
      max-width: none;
      border-radius: 0 0 var(--radius-sm) var(--radius-sm);
    }
  }
</style>
